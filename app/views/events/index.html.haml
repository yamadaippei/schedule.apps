%h1 スケジュール・カレンダー

#calendar

-# %table
-#   %th
-#     %tr
-#       %th タイトル
-#       %th 内容
-#       %th 開始時間
-#       %th 終了時間


  -# %tbody
  -#   - @events.each do |event|
  -#     %tr
  -#       %td= event.title
  -#       %td= event.body
  -#       %td= event.start_date.to_s(:datetime_jp)
  -#       %td= event.end_date.to_s(:datetime_jp)
  -#       %td= link_to '内容確認', event
  -#       %td= link_to '編集', edit_event_path(event)
  -#       %td= link_to '消去', event, method: :delete, data: { confirm: '本当消去しますか？' }

%br

.create_btn
  = link_to '新規イベント作成', new_event_path
